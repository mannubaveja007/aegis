# LLM — Gemini (reasoning + sanitizer)
GOOGLE_GENERATIVE_AI_API_KEY=

# Jev gate — OpenRouter System One API
OPENROUTER_API_KEY=

# Stripe, Jira, Gmail, Slack, Notion are managed by Swytchcode.
# Run `swy auth connect <provider>` for each — no keys needed here.
